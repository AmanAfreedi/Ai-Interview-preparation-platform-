rules_version = '2';

// Resume PDFs are not stored — only extracted text in Firestore.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
